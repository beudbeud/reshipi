<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature android:name="android.hardware.usb.host" android:required="false" />
    <!-- Web recipe import (Fuji X Weekly links shared from the browser) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- A LUT export holds a 56 MB uncompressed RAF and both 3120x2080 renders
         at once, around 110 MB — past the default heap on modest devices. -->
    <application
        android:largeHeap="true"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/Theme.Fuji"
        android:allowBackup="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Share target: recipe text, a recipe page URL, or an image (camera JPEG / FujiStyle card) -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
                <data android:mimeType="image/*" />
            </intent-filter>
        </activity>
    </application>

</manifest>
